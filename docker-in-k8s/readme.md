# How to run docker commands inside Kubernetes cluster
kubectl apply -f https://raw.githubusercontent.com/brainupgrade-in/kubernetes/main/docker-in-k8s/docker-pod.yaml && kubectl exec -it docker -- sh
